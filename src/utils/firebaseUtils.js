const FirebaseUtils = {
  // Stub methods to prevent crash if still referenced, but should be unused.
  validateEmail: () => true,
  validatePassword: () => ({ isValid: true }),
  validateDisplayName: () => ({ isValid: true }),
};
export default FirebaseUtils;
